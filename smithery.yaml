# Smithery configuration for faida-mcp
# East Africa AI Coordination Infrastructure — AI-KungFU
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({
      command: "faida-mcp",
      args: [],
      env: {}
    })
